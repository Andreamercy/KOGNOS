{
  "id": "INC-2024-11-03",
  "title": "Payment service OOMKill cascade during Black Friday load",
  "service": "payment-svc",
  "severity": "P1",
  "start_time": "2024-11-03T14:22:00Z",
  "resolution_time": "2024-11-03T15:07:00Z",
  "duration_minutes": 45,
  "responders": ["andrea.mercy@example.com", "sre-team@example.com"],
  "root_cause": "Memory leak in payment processor v2.3.0 under sustained high load. The JVM heap was configured at 512Mi but the service was consuming up to 800Mi during peak checkout flows. Kubernetes OOM killer terminated the pod before requests could complete, causing a cascade of retry storms that amplified load on the remaining replicas.",
  "contributing_factors": [
    "JVM heap not tuned to container memory limits (-Xmx not set)",
    "HPA min replicas too low (3) for Black Friday traffic",
    "No circuit breaker between cart-svc and payment-svc",
    "Missing alert on OOMKill events (only alerted on HTTP 500 rate)"
  ],
  "symptoms": [
    "OOMKill events in pod events at 14:22 UTC",
    "HTTP 500 error rate on payment-svc exceeded 40% within 3 minutes",
    "Cart abandonment rate spiked 85% above baseline",
    "Checkout latency p99 exceeded 30 seconds",
    "KOGNOS anomaly score for payment-svc: 0.94 (CRITICAL)"
  ],
  "timeline": [
    {"time": "14:22", "event": "First OOMKill event on payment-svc-7d9f8b-xk2pq"},
    {"time": "14:24", "event": "KOGNOS CRITICAL alert fired (score=0.91)"},
    {"time": "14:26", "event": "PagerDuty page sent to on-call engineer"},
    {"time": "14:31", "event": "Engineer acknowledged, began investigation"},
    {"time": "14:38", "event": "Root cause identified: OOMKill from JVM heap exhaustion"},
    {"time": "14:41", "event": "kubectl scale deployment/payment-svc --replicas=8"},
    {"time": "14:50", "event": "Error rate dropping, replicas healthy"},
    {"time": "15:07", "event": "Error rate < 1%, incident resolved"}
  ],
  "resolution": "Scaled payment-svc from 3 to 8 replicas to immediately reduce per-pod load. Applied JVM heap fix (-Xmx1g) via config change. Deployed patched image v2.3.1 with memory leak fix. Verified KOGNOS anomaly score dropped from 0.94 to 0.08 post-resolution.",
  "kubectl_commands": [
    "kubectl describe pod payment-svc-7d9f8b-xk2pq -n production",
    "kubectl logs payment-svc-7d9f8b-xk2pq -n production --tail=50",
    "kubectl scale deployment/payment-svc --replicas=8 -n production",
    "kubectl get pods -l app=payment-svc -n production -w",
    "kubectl top pod -l app=payment-svc -n production",
    "kubectl set env deployment/payment-svc JAVA_OPTS='-Xms512m -Xmx1g' -n production"
  ],
  "action_items": [
    {"item": "Set JVM heap via JAVA_OPTS in all Java service deployments", "owner": "platform-eng", "due": "2024-11-10"},
    {"item": "Add HPA for payment-svc (target CPU 70%, min=5 replicas)", "owner": "platform-eng", "due": "2024-11-10"},
    {"item": "Add Prometheus alert for OOMKill events", "owner": "observability-team", "due": "2024-11-07"},
    {"item": "Add circuit breaker on cart-svc → payment-svc", "owner": "payments-team", "due": "2024-11-17"},
    {"item": "Load test payment-svc at 2x expected Black Friday load", "owner": "qa-team", "due": "2024-11-17"}
  ],
  "lessons_learned": "KOGNOS detected the anomaly 4 minutes before the PagerDuty alert fired. The GNN correctly identified payment-svc as the root cause (score=0.94) while cart-svc showed secondary anomaly (score=0.76). Future: wire KOGNOS auto-heal to scale payment-svc automatically when score > 0.9 and reason matches OOMKill pattern."
}
